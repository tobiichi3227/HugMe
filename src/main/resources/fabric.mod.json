{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": [
    "${mod_authors}"
  ],
  "license": "${mod_license}",
  "icon": "hug_ticket.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "nya.tuyw.hugme.HugMe"
    ],
    "client": [
      "nya.tuyw.hugme.HugMeClient"
    ]
  },
  "mixins": [
    "hugme.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.15.0",
    "fabric-api": "*",
    "minecraft": ">=1.21.1",
    "playeranimator": ">=2.0.0"
  }
}
